### Bash terminal
conda create --name uts python=3.8 -y
conda activate uts

cd Streaming/

# ONLY IF NECESSARY (ONE OR THE OTHER)
pip install -r requirements.txt
sudo apt install ffmpeg
sudo pip3 install qdarkstyle
sudo pip3 install opencv-python
sudo pip3 install imutils
sudo pip3 install PySide
conda install qt --force

### Python terminal (ctrl+shift+p)
conda activate uts
cd Streaming/

/bin/python3 multiple_stream_gui.py
